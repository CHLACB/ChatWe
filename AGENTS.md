# AGENTS.md

## 项目目标

构建 Windows 本地微信 AI 自动回复系统。系统通过 FastAPI 后端、Windows UI Automation、本地历史消息读取、AI 网关预留、发送队列，实现监听名单内好友/群聊文本消息的采集、上下文构建、AI 回复生成和自动发送。

## 硬性约束

1. 分层架构必须保持：API 层、应用服务层、监听调度层、消息采集层、历史读取层、身份验证层、消息存储层、上下文构建层、AI 网关层、发送队列层、微信执行层、环境检测层。
2. API 层只做外部入口，不写业务流程，不直接调用 Driver。
3. 不允许调用第三方 wxautox4 HTTP API。
4. 不允许后端内部 HTTP 调自己的 API。
5. 不允许凭猜测写死 UIA 控件路径、固定序号、固定层级。
6. 允许把已通过本机控件树 dump、Inspect.exe、Accessibility Insights 或实际运行验证的规律写成版本专用策略。
7. 当前版本专用目标：微信 PC 版 3.9.12.56。
8. 第一阶段只支持文本消息。
9. 所有发送必须进入发送队列，微信窗口操作必须串行执行。
10. 入库前、发送前、发送后必须做 who / conversation 身份验证。
11. 自己发出的消息必须入库，但不能触发 AI。
12. AI 网关只生成最终要发送的文本，不允许直接发送微信。
13. Driver 层不允许调用 AI。
14. 监听循环不允许直接发送消息，只能创建发送任务。

## UIA 固化规则要求

写入微信 3.9.12.56 专用定位策略时，必须同时记录：

1. 适用微信版本。
2. 验证来源。
3. 失败兜底逻辑。
4. 失败时输出哪些采集信息。

未知控件必须先采集再固化；定位失败时返回结构化错误和采集命令，不继续猜。
